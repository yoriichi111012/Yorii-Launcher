<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Yorii_Launcher.Pages.InstalledModpacksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">

    <Grid>
        <Border Padding="12">
            <Grid
                AllowDrop="True"
                DragOver="Modpack_DragOver"
                Drop="Modpack_Drop">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="24"
                    FontWeight="SemiBold"
                    Text="Installed Modpacks" />

                <Grid
                    Grid.Row="1"
                    MaxWidth="592"
                    Margin="0,8,0,8"
                    HorizontalAlignment="Center"
                    ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="480" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox
                        x:Name="ModpacksSearchBox"
                        Grid.Column="0"
                        PlaceholderText="Search Installed Modpacks..."
                        TextChanged="ModpacksSearchBox_TextChanged" />

                    <controls:Segmented
                        x:Name="ModpacksViewModeSegmented"
                        Grid.Column="1"
                        HorizontalAlignment="Right"
                        SelectionChanged="ViewModeSegmented_SelectionChanged">
                        <controls:SegmentedItem
                            AutomationProperties.Name="List view"
                            Icon="{ui:FontIcon Glyph=&#xE8FD;}" />
                        <controls:SegmentedItem
                            AutomationProperties.Name="Grid view"
                            Icon="{ui:FontIcon Glyph=&#xE8A9;}" />
                    </controls:Segmented>
                </Grid>

                <TextBlock
                    x:Name="ModpacksErrorText"
                    Grid.Row="2"
                    Margin="0,12,0,0"
                    HorizontalAlignment="Center"
                    FontSize="13"
                    Opacity="0.7"
                    Text="Could not open modpack."
                    Visibility="Collapsed" />

                <ListView
                    x:Name="ModpacksList"
                    Grid.Row="2"
                    Margin="0,0,0,4"
                    Padding="24,0,24,0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    BorderThickness="0"
                    SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border
                                MaxWidth="1200"
                                Margin="0,0,0,6"
                                Padding="10"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                CornerRadius="4">
                                <Border.ContextFlyout>
                                    <MenuFlyout>
                                        <MenuFlyoutItem Click="OpenFile_Click" Text="Reveal in Explorer" />
                                        <MenuFlyoutItem Click="OpenFolder_Click" Text="Open Modpacks Folder" />
                                        <MenuFlyoutItem Click="OpenModrinth_Click" Text="Open Modrinth Page" />
                                        <MenuFlyoutSeparator />
                                        <MenuFlyoutItem
                                            Click="DeleteModpack_Click"
                                            Foreground="#FF5555"
                                            Text="Delete Modpack" />
                                    </MenuFlyout>
                                </Border.ContextFlyout>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel
                                        Margin="6,0,0,0"
                                        Orientation="Horizontal"
                                        Spacing="16">
                                        <FontIcon
                                            Width="40"
                                            Height="40"
                                            FontSize="24"
                                            Foreground="{ThemeResource SystemAccentColor}"
                                            Glyph="&#xE8FD;" />
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock
                                                FontSize="14"
                                                FontWeight="SemiBold"
                                                Text="{Binding Name}" />
                                            <TextBlock
                                                FontSize="12"
                                                Opacity="0.7"
                                                Text="{Binding Version}" />
                                        </StackPanel>
                                    </StackPanel>

                                    <ToggleSwitch
                                        Grid.Column="1"
                                        Width="50"
                                        MinWidth="0"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        IsOn="{Binding IsEnabled}"
                                        OffContent=""
                                        OnContent=""
                                        Toggled="ToggleSwitch_Toggled" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
