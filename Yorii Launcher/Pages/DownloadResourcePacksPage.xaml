<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Yorii_Launcher.Pages.DownloadResourcePacksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">

    <Grid>
        <Border Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="24"
                    FontWeight="SemiBold"
                    Text="Discover Resource Packs" />

                <Grid
                    Grid.Row="1"
                    MaxWidth="592"
                    Margin="0,8,0,8"
                    HorizontalAlignment="Center"
                    ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="480" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox
                        x:Name="ResourcePackSearchBox"
                        Grid.Column="0"
                        PlaceholderText="Search Modrinth..."
                        TextChanged="ResourcePackSearchBox_TextChanged" />

                    <controls:Segmented
                        x:Name="ResourcePacksViewModeSegmented"
                        Grid.Column="1"
                        HorizontalAlignment="Right"
                        SelectionChanged="ViewModeSegmented_SelectionChanged">
                        <controls:SegmentedItem
                            AutomationProperties.Name="List view"
                            Icon="{ui:FontIcon Glyph=&#xE8FD;}" />
                        <controls:SegmentedItem
                            AutomationProperties.Name="Grid view"
                            Icon="{ui:FontIcon Glyph=&#xE8A9;}" />
                    </controls:Segmented>
                </Grid>

                <StackPanel
                    x:Name="ResourcePacksErrorPanel"
                    Grid.Row="2"
                    Margin="0,12,0,0"
                    HorizontalAlignment="Center"
                    Orientation="Vertical"
                    Spacing="8"
                    Visibility="Collapsed">
                    <TextBlock
                        HorizontalAlignment="Center"
                        FontSize="13"
                        Opacity="0.7"
                        Text="Could not connect to Modrinth. Check your internet connection." />
                    <Button
                        Padding="16,4"
                        HorizontalAlignment="Center"
                        Click="ResourcePacksRetryButton_Click"
                        Style="{ThemeResource AccentButtonStyle}">
                        <TextBlock VerticalAlignment="Center" Text="Retry" />
                    </Button>
                </StackPanel>

                <ListView
                    x:Name="ResourcePacksList"
                    Grid.Row="2"
                    Margin="0,0,0,4"
                    Padding="24,0,24,0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    BorderThickness="0"
                    SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Button
                                MaxWidth="1200"
                                Margin="0,0,0,6"
                                Padding="0"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Stretch"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                Click="OpenOnlineResourcePack_Click"
                                CornerRadius="4"
                                Tag="{Binding}">
                                <Border
                                    MaxWidth="1200"
                                    Padding="10"
                                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="auto" />
                                            <ColumnDefinition Width="auto" />
                                        </Grid.ColumnDefinitions>

                                        <Grid Grid.Column="0" ColumnSpacing="16">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Image
                                                Grid.Column="0"
                                                Width="40"
                                                Height="40"
                                                Margin="6,0,0,0"
                                                Source="{Binding Icon}" />
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock
                                                    FontSize="14"
                                                    FontWeight="SemiBold"
                                                    Text="{Binding Title}" />
                                                <TextBlock
                                                    Margin="0,0,8,0"
                                                    FontSize="12"
                                                    Opacity="0.7"
                                                    Text="{Binding Description}"
                                                    MaxLines="2" TextTrimming="CharacterEllipsis" TextWrapping="WrapWholeWords" />
                                            </StackPanel>
                                        </Grid>

                                        <Button
                                            Grid.Column="1"
                                            Height="30"
                                            Padding="10,0,10,0"
                                            Click="InstallResourcePack_Click"
                                            Style="{ThemeResource AccentButtonStyle}"
                                            Tag="{Binding}">
                                            <TextBlock
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                Text="Install" />
                                        </Button>

                                        <Button
                                            Grid.Column="2"
                                            Width="30"
                                            Height="30"
                                            Margin="4,0,0,0"
                                            Padding="0"
                                            Click="ShowVersions_Click"
                                            Tag="{Binding}">
                                            <FontIcon
                                                FontSize="16"
                                                Glyph="&#xE70D;"
                                                ToolTipService.ToolTip="Versions" />
                                        </Button>
                                    </Grid>
                                </Border>
                            </Button>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
